# Patchi Security Domain: mitre-lateral-movement-use-alternate-auth
# Source: MITRE ATT&CK
# Auto-generated — curated from MITRE ATT&CK

domain_id: "mitre-lateral-movement-use-alternate-auth"
version: "1.0.0"
display_name: "Use Alternate Authentication Material (T1550)"
source_standard: "MITRE ATT&CK"
component_type: "backend-api"
weight: 0.8

activation_signals:
  required_any:
    - signal: "code_in_project_matches_mitre_lateral_movement_use_alternate_auth_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_mitre_lateral_movement_use_alternate_auth"

controls:
  - control_id: "MITRELAT-01"
    name: "Use Alternate Authentication Material (T1550) — Primary Control"
    description: "Using stolen tokens, cookies, or hashes for lateral movement."
    source_clause: "T1550"
    severity: "high"
    check_method: "static+dynamic"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://MITRELAT-01"

  - control_id: "MITRELAT-02"
    name: "Use Alternate Authentication Material (T1550) — Input Validation"
    description: "Validate and sanitize all inputs related to use alternate authentication material (t1550)"
    source_clause: "T1550"
    severity: "high"
    check_method: "static+dynamic"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://MITRELAT-02"

  - control_id: "MITRELAT-03"
    name: "Use Alternate Authentication Material (T1550) — Configuration Hardening"
    description: "Apply secure configuration defaults for use alternate authentication material (t1550)"
    source_clause: "T1550"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://MITRELAT-03"

  - control_id: "MITRELAT-04"
    name: "Use Alternate Authentication Material (T1550) — Monitoring and Logging"
    description: "Log and monitor all use alternate authentication material (t1550) events for audit and detection"
    source_clause: "T1550"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://MITRELAT-04"

  - control_id: "MITRELAT-05"
    name: "Use Alternate Authentication Material (T1550) — Error Handling"
    description: "Handle use alternate authentication material (t1550) errors securely without information leakage"
    source_clause: "T1550"
    severity: "medium"
    check_method: "static+dynamic"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://MITRELAT-05"
