# Patchi Security Domain: wasm-sandbox-escape
# Source: WASM Security
# Auto-generated — curated from WASM Security

domain_id: "wasm-sandbox-escape"
version: "1.0.0"
display_name: "WASM Sandbox Escape"
source_standard: "WASM Security"
component_type: "frontend-web"
weight: 0.85

activation_signals:
  required_any:
    - signal: "code_in_project_matches_wasm_sandbox_escape_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_wasm_sandbox_escape"

controls:
  - control_id: "WASMSAND-01"
    name: "WASM Sandbox Escape — Primary Control"
    description: "WebAssembly module breaking out of sandbox via host function abuse."
    source_clause: "WASM Escape"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://WASMSAND-01"

  - control_id: "WASMSAND-02"
    name: "WASM Sandbox Escape — Input Validation"
    description: "Validate and sanitize all inputs related to wasm sandbox escape"
    source_clause: "WASM Escape"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://WASMSAND-02"

  - control_id: "WASMSAND-03"
    name: "WASM Sandbox Escape — Configuration Hardening"
    description: "Apply secure configuration defaults for wasm sandbox escape"
    source_clause: "WASM Escape"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://WASMSAND-03"

  - control_id: "WASMSAND-04"
    name: "WASM Sandbox Escape — Monitoring and Logging"
    description: "Log and monitor all wasm sandbox escape events for audit and detection"
    source_clause: "WASM Escape"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://WASMSAND-04"

  - control_id: "WASMSAND-05"
    name: "WASM Sandbox Escape — Error Handling"
    description: "Handle wasm sandbox escape errors securely without information leakage"
    source_clause: "WASM Escape"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://WASMSAND-05"
