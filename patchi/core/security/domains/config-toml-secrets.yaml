# Patchi Security Domain: config-toml-secrets
# Source: Configuration Security
# Auto-generated — curated from Configuration Security

domain_id: "config-toml-secrets"
version: "1.0.0"
display_name: "TOML Configuration Secrets"
source_standard: "Configuration Security"
component_type: "infra"
weight: 0.8

activation_signals:
  required_any:
    - signal: "config_file_detected"
    - signal: "docker_file_detected"
    - signal: "yaml_manifest_detected"
    - signal: "secret_management_present"
  excluded_if:
    - signal: "no_relevant_code_for_infra"

controls:
  - control_id: "CONFIGTO-01"
    name: "TOML Configuration Secrets — Primary Control"
    description: "Secrets in pyproject.toml, Cargo.toml, or config.toml."
    source_clause: "Config TOML"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://CONFIGTO-01"

  - control_id: "CONFIGTO-02"
    name: "TOML Configuration Secrets — Input Validation"
    description: "Validate and sanitize all inputs related to toml configuration secrets"
    source_clause: "Config TOML"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://CONFIGTO-02"

  - control_id: "CONFIGTO-03"
    name: "TOML Configuration Secrets — Configuration Hardening"
    description: "Apply secure configuration defaults for toml configuration secrets"
    source_clause: "Config TOML"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://CONFIGTO-03"

  - control_id: "CONFIGTO-04"
    name: "TOML Configuration Secrets — Monitoring and Logging"
    description: "Log and monitor all toml configuration secrets events for audit and detection"
    source_clause: "Config TOML"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://CONFIGTO-04"

  - control_id: "CONFIGTO-05"
    name: "TOML Configuration Secrets — Error Handling"
    description: "Handle toml configuration secrets errors securely without information leakage"
    source_clause: "Config TOML"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://CONFIGTO-05"
