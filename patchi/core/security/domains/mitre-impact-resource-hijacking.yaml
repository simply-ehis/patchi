# Patchi Security Domain: mitre-impact-resource-hijacking
# Source: MITRE ATT&CK
# Auto-generated — curated from MITRE ATT&CK

domain_id: "mitre-impact-resource-hijacking"
version: "1.0.0"
display_name: "Resource Hijacking (T1496)"
source_standard: "MITRE ATT&CK"
component_type: "infra"
weight: 0.8

activation_signals:
  required_any:
    - signal: "code_in_project_matches_mitre_impact_resource_hijacking_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_mitre_impact_resource_hijacking"

controls:
  - control_id: "MITREIMP-01"
    name: "Resource Hijacking (T1496) — Primary Control"
    description: "Using compromised resources for cryptocurrency mining."
    source_clause: "T1496"
    severity: "high"
    check_method: "dynamic"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://MITREIMP-01"

  - control_id: "MITREIMP-02"
    name: "Resource Hijacking (T1496) — Input Validation"
    description: "Validate and sanitize all inputs related to resource hijacking (t1496)"
    source_clause: "T1496"
    severity: "high"
    check_method: "dynamic"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://MITREIMP-02"

  - control_id: "MITREIMP-03"
    name: "Resource Hijacking (T1496) — Configuration Hardening"
    description: "Apply secure configuration defaults for resource hijacking (t1496)"
    source_clause: "T1496"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://MITREIMP-03"

  - control_id: "MITREIMP-04"
    name: "Resource Hijacking (T1496) — Monitoring and Logging"
    description: "Log and monitor all resource hijacking (t1496) events for audit and detection"
    source_clause: "T1496"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://MITREIMP-04"

  - control_id: "MITREIMP-05"
    name: "Resource Hijacking (T1496) — Error Handling"
    description: "Handle resource hijacking (t1496) errors securely without information leakage"
    source_clause: "T1496"
    severity: "medium"
    check_method: "dynamic"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://MITREIMP-05"
