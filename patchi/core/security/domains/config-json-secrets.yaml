# Patchi Security Domain: config-json-secrets
# Source: Configuration Security
# Auto-generated — curated from Configuration Security

domain_id: "config-json-secrets"
version: "1.0.0"
display_name: "JSON Configuration Secrets"
source_standard: "Configuration Security"
component_type: "infra"
weight: 0.85

activation_signals:
  required_any:
    - signal: "config_file_detected"
    - signal: "docker_file_detected"
    - signal: "yaml_manifest_detected"
    - signal: "secret_management_present"
  excluded_if:
    - signal: "no_relevant_code_for_infra"

controls:
  - control_id: "CONFIGJS-01"
    name: "JSON Configuration Secrets — Primary Control"
    description: "Secrets in package.json, config.json, or settings files."
    source_clause: "Config JSON"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://CONFIGJS-01"

  - control_id: "CONFIGJS-02"
    name: "JSON Configuration Secrets — Input Validation"
    description: "Validate and sanitize all inputs related to json configuration secrets"
    source_clause: "Config JSON"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://CONFIGJS-02"

  - control_id: "CONFIGJS-03"
    name: "JSON Configuration Secrets — Configuration Hardening"
    description: "Apply secure configuration defaults for json configuration secrets"
    source_clause: "Config JSON"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://CONFIGJS-03"

  - control_id: "CONFIGJS-04"
    name: "JSON Configuration Secrets — Monitoring and Logging"
    description: "Log and monitor all json configuration secrets events for audit and detection"
    source_clause: "Config JSON"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://CONFIGJS-04"

  - control_id: "CONFIGJS-05"
    name: "JSON Configuration Secrets — Error Handling"
    description: "Handle json configuration secrets errors securely without information leakage"
    source_clause: "Config JSON"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://CONFIGJS-05"
