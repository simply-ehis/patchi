# Patchi Security Domain: config-yaml-secrets
# Source: Configuration Security
# Auto-generated — curated from Configuration Security

domain_id: "config-yaml-secrets"
version: "1.0.0"
display_name: "YAML Configuration Secrets"
source_standard: "Configuration Security"
component_type: "infra"
weight: 0.85

activation_signals:
  required_any:
    - signal: "code_in_project_matches_config_yaml_secrets_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_config_yaml_secrets"

controls:
  - control_id: "CONFIGYA-01"
    name: "YAML Configuration Secrets — Primary Control"
    description: "Secrets in docker-compose.yml, kubernetes manifests, or CI configs."
    source_clause: "Config YAML"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://CONFIGYA-01"

  - control_id: "CONFIGYA-02"
    name: "YAML Configuration Secrets — Input Validation"
    description: "Validate and sanitize all inputs related to yaml configuration secrets"
    source_clause: "Config YAML"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://CONFIGYA-02"

  - control_id: "CONFIGYA-03"
    name: "YAML Configuration Secrets — Configuration Hardening"
    description: "Apply secure configuration defaults for yaml configuration secrets"
    source_clause: "Config YAML"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://CONFIGYA-03"

  - control_id: "CONFIGYA-04"
    name: "YAML Configuration Secrets — Monitoring and Logging"
    description: "Log and monitor all yaml configuration secrets events for audit and detection"
    source_clause: "Config YAML"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://CONFIGYA-04"

  - control_id: "CONFIGYA-05"
    name: "YAML Configuration Secrets — Error Handling"
    description: "Handle yaml configuration secrets errors securely without information leakage"
    source_clause: "Config YAML"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://CONFIGYA-05"
