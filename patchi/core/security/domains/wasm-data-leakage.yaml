# Patchi Security Domain: wasm-data-leakage
# Source: WASM Security
# Auto-generated — curated from WASM Security

domain_id: "wasm-data-leakage"
version: "1.0.0"
display_name: "WASM Data Leakage"
source_standard: "WASM Security"
component_type: "frontend-web"
weight: 0.8

activation_signals:
  required_any:
    - signal: "code_in_project_matches_wasm_data_leakage_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_wasm_data_leakage"

controls:
  - control_id: "WASMDATA-01"
    name: "WASM Data Leakage — Primary Control"
    description: "Sensitive data accessible through WASM linear memory."
    source_clause: "WASM Leak"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://WASMDATA-01"

  - control_id: "WASMDATA-02"
    name: "WASM Data Leakage — Input Validation"
    description: "Validate and sanitize all inputs related to wasm data leakage"
    source_clause: "WASM Leak"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://WASMDATA-02"

  - control_id: "WASMDATA-03"
    name: "WASM Data Leakage — Configuration Hardening"
    description: "Apply secure configuration defaults for wasm data leakage"
    source_clause: "WASM Leak"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://WASMDATA-03"

  - control_id: "WASMDATA-04"
    name: "WASM Data Leakage — Monitoring and Logging"
    description: "Log and monitor all wasm data leakage events for audit and detection"
    source_clause: "WASM Leak"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://WASMDATA-04"

  - control_id: "WASMDATA-05"
    name: "WASM Data Leakage — Error Handling"
    description: "Handle wasm data leakage errors securely without information leakage"
    source_clause: "WASM Leak"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://WASMDATA-05"
