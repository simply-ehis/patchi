# Patchi Security Domain: database-encryption-in-transit
# Source: Database Security
# Auto-generated — curated from Database Security

domain_id: "database-encryption-in-transit"
version: "1.0.0"
display_name: "Database Encryption in Transit"
source_standard: "Database Security"
component_type: "infra"
weight: 0.9

activation_signals:
  required_any:
    - signal: "config_file_detected"
    - signal: "docker_file_detected"
    - signal: "yaml_manifest_detected"
  excluded_if:
    - signal: "no_relevant_code_for_infra"

controls:
  - control_id: "DATABASE-01"
    name: "Database Encryption in Transit — Primary Control"
    description: "Database connections without TLS or with certificate verification disabled."
    source_clause: "DB TLS"
    severity: "critical"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://DATABASE-01"

  - control_id: "DATABASE-02"
    name: "Database Encryption in Transit — Input Validation"
    description: "Validate and sanitize all inputs related to database encryption in transit"
    source_clause: "DB TLS"
    severity: "critical"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://DATABASE-02"

  - control_id: "DATABASE-03"
    name: "Database Encryption in Transit — Configuration Hardening"
    description: "Apply secure configuration defaults for database encryption in transit"
    source_clause: "DB TLS"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://DATABASE-03"

  - control_id: "DATABASE-04"
    name: "Database Encryption in Transit — Monitoring and Logging"
    description: "Log and monitor all database encryption in transit events for audit and detection"
    source_clause: "DB TLS"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://DATABASE-04"

  - control_id: "DATABASE-05"
    name: "Database Encryption in Transit — Error Handling"
    description: "Handle database encryption in transit errors securely without information leakage"
    source_clause: "DB TLS"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://DATABASE-05"
