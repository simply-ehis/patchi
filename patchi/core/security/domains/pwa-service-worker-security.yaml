# Patchi Security Domain: pwa-service-worker-security
# Source: PWA Security
# Auto-generated — curated from PWA Security

domain_id: "pwa-service-worker-security"
version: "1.0.0"
display_name: "PWA Service Worker Security"
source_standard: "PWA Security"
component_type: "frontend-web"
weight: 0.8

activation_signals:
  required_any:
    - signal: "javascript_file_detected"
    - signal: "html_template_detected"
    - signal: "css_file_detected"
  excluded_if:
    - signal: "no_relevant_code_for_frontend-web"

controls:
  - control_id: "PWASERVI-01"
    name: "PWA Service Worker Security — Primary Control"
    description: "Service worker caching sensitive data or serving stale content."
    source_clause: "PWA SW"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://PWASERVI-01"

  - control_id: "PWASERVI-02"
    name: "PWA Service Worker Security — Input Validation"
    description: "Validate and sanitize all inputs related to pwa service worker security"
    source_clause: "PWA SW"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://PWASERVI-02"

  - control_id: "PWASERVI-03"
    name: "PWA Service Worker Security — Configuration Hardening"
    description: "Apply secure configuration defaults for pwa service worker security"
    source_clause: "PWA SW"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://PWASERVI-03"

  - control_id: "PWASERVI-04"
    name: "PWA Service Worker Security — Monitoring and Logging"
    description: "Log and monitor all pwa service worker security events for audit and detection"
    source_clause: "PWA SW"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://PWASERVI-04"

  - control_id: "PWASERVI-05"
    name: "PWA Service Worker Security — Error Handling"
    description: "Handle pwa service worker security errors securely without information leakage"
    source_clause: "PWA SW"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://PWASERVI-05"
