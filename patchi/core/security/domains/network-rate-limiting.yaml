# Patchi Security Domain: network-rate-limiting
# Source: Network Security
# Auto-generated — curated from Network Security

domain_id: "network-rate-limiting"
version: "1.0.0"
display_name: "Rate Limiting"
source_standard: "Network Security"
component_type: "infra"
weight: 0.85

activation_signals:
  required_any:
    - signal: "config_file_detected"
    - signal: "docker_file_detected"
    - signal: "yaml_manifest_detected"
  excluded_if:
    - signal: "no_relevant_code_for_infra"

controls:
  - control_id: "NETWORKR-01"
    name: "Rate Limiting — Primary Control"
    description: "Missing or ineffective rate limiting on API endpoints."
    source_clause: "Net RateLimit"
    severity: "high"
    check_method: "dynamic"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://NETWORKR-01"

  - control_id: "NETWORKR-02"
    name: "Rate Limiting — Input Validation"
    description: "Validate and sanitize all inputs related to rate limiting"
    source_clause: "Net RateLimit"
    severity: "high"
    check_method: "dynamic"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://NETWORKR-02"

  - control_id: "NETWORKR-03"
    name: "Rate Limiting — Configuration Hardening"
    description: "Apply secure configuration defaults for rate limiting"
    source_clause: "Net RateLimit"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://NETWORKR-03"

  - control_id: "NETWORKR-04"
    name: "Rate Limiting — Monitoring and Logging"
    description: "Log and monitor all rate limiting events for audit and detection"
    source_clause: "Net RateLimit"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://NETWORKR-04"

  - control_id: "NETWORKR-05"
    name: "Rate Limiting — Error Handling"
    description: "Handle rate limiting errors securely without information leakage"
    source_clause: "Net RateLimit"
    severity: "medium"
    check_method: "dynamic"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://NETWORKR-05"
