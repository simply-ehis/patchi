# Patchi Security Domain: sans-hardcoded-credentials
# Source: SANS Top 25
# Auto-generated — curated from SANS Top 25

domain_id: "sans-hardcoded-credentials"
version: "1.0.0"
display_name: "Hard-coded Credentials"
source_standard: "SANS Top 25"
component_type: "backend-api"
weight: 0.9

activation_signals:
  required_any:
    - signal: "code_in_project_matches_sans_hardcoded_credentials_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_sans_hardcoded_credentials"

controls:
  - control_id: "SANSHARD-01"
    name: "Hard-coded Credentials — Primary Control"
    description: "Passwords, keys, or tokens embedded in source code."
    source_clause: "SANS"
    severity: "critical"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://SANSHARD-01"

  - control_id: "SANSHARD-02"
    name: "Hard-coded Credentials — Input Validation"
    description: "Validate and sanitize all inputs related to hard-coded credentials"
    source_clause: "SANS"
    severity: "critical"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://SANSHARD-02"

  - control_id: "SANSHARD-03"
    name: "Hard-coded Credentials — Configuration Hardening"
    description: "Apply secure configuration defaults for hard-coded credentials"
    source_clause: "SANS"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://SANSHARD-03"

  - control_id: "SANSHARD-04"
    name: "Hard-coded Credentials — Monitoring and Logging"
    description: "Log and monitor all hard-coded credentials events for audit and detection"
    source_clause: "SANS"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://SANSHARD-04"

  - control_id: "SANSHARD-05"
    name: "Hard-coded Credentials — Error Handling"
    description: "Handle hard-coded credentials errors securely without information leakage"
    source_clause: "SANS"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://SANSHARD-05"
