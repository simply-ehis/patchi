# Patchi Security Domain: secrets-in-artifacts
# Source: Secrets Management
# Auto-generated — curated from Secrets Management

domain_id: "secrets-in-artifacts"
version: "1.0.0"
display_name: "Secrets in Build Artifacts"
source_standard: "Secrets Management"
component_type: "infra"
weight: 0.85

activation_signals:
  required_any:
    - signal: "code_in_project_matches_secrets_in_artifacts_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_secrets_in_artifacts"

controls:
  - control_id: "SECRETSI-01"
    name: "Secrets in Build Artifacts — Primary Control"
    description: "Secrets embedded in Docker images, JARs, or other build artifacts."
    source_clause: "Secrets Artifact"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://SECRETSI-01"

  - control_id: "SECRETSI-02"
    name: "Secrets in Build Artifacts — Input Validation"
    description: "Validate and sanitize all inputs related to secrets in build artifacts"
    source_clause: "Secrets Artifact"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://SECRETSI-02"

  - control_id: "SECRETSI-03"
    name: "Secrets in Build Artifacts — Configuration Hardening"
    description: "Apply secure configuration defaults for secrets in build artifacts"
    source_clause: "Secrets Artifact"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://SECRETSI-03"

  - control_id: "SECRETSI-04"
    name: "Secrets in Build Artifacts — Monitoring and Logging"
    description: "Log and monitor all secrets in build artifacts events for audit and detection"
    source_clause: "Secrets Artifact"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://SECRETSI-04"

  - control_id: "SECRETSI-05"
    name: "Secrets in Build Artifacts — Error Handling"
    description: "Handle secrets in build artifacts errors securely without information leakage"
    source_clause: "Secrets Artifact"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://SECRETSI-05"
