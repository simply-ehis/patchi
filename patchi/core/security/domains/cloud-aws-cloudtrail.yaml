# Patchi Security Domain: cloud-aws-cloudtrail
# Source: Cloud Security AWS
# Auto-generated — curated from Cloud Security AWS

domain_id: "cloud-aws-cloudtrail"
version: "1.0.0"
display_name: "AWS CloudTrail Configuration"
source_standard: "Cloud Security AWS"
component_type: "cloud-aws"
weight: 0.85

activation_signals:
  required_any:
    - signal: "code_in_project_matches_cloud_aws_cloudtrail_pattern"
  excluded_if:
    - signal: "no_relevant_code_for_cloud_aws_cloudtrail"

controls:
  - control_id: "CLOUDAWS-01"
    name: "AWS CloudTrail Configuration — Primary Control"
    description: "CloudTrail not enabled or logging to unencrypted S3 bucket."
    source_clause: "AWS CloudTrail"
    severity: "high"
    check_method: "static"
    detector: "static analysis + semantic pattern matching"
    remediation_ref: "fix-playbook://CLOUDAWS-01"

  - control_id: "CLOUDAWS-02"
    name: "AWS CloudTrail Configuration — Input Validation"
    description: "Validate and sanitize all inputs related to aws cloudtrail configuration"
    source_clause: "AWS CloudTrail"
    severity: "high"
    check_method: "static"
    detector: "input validation pattern analysis"
    remediation_ref: "fix-playbook://CLOUDAWS-02"

  - control_id: "CLOUDAWS-03"
    name: "AWS CloudTrail Configuration — Configuration Hardening"
    description: "Apply secure configuration defaults for aws cloudtrail configuration"
    source_clause: "AWS CloudTrail"
    severity: "medium"
    check_method: "static"
    detector: "configuration analysis"
    remediation_ref: "fix-playbook://CLOUDAWS-03"

  - control_id: "CLOUDAWS-04"
    name: "AWS CloudTrail Configuration — Monitoring and Logging"
    description: "Log and monitor all aws cloudtrail configuration events for audit and detection"
    source_clause: "AWS CloudTrail"
    severity: "medium"
    check_method: "static"
    detector: "logging pattern analysis"
    remediation_ref: "fix-playbook://CLOUDAWS-04"

  - control_id: "CLOUDAWS-05"
    name: "AWS CloudTrail Configuration — Error Handling"
    description: "Handle aws cloudtrail configuration errors securely without information leakage"
    source_clause: "AWS CloudTrail"
    severity: "medium"
    check_method: "static"
    detector: "error handling pattern analysis"
    remediation_ref: "fix-playbook://CLOUDAWS-05"
