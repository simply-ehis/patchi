# Playbook for: secrets-file-exposure
# Source: Secrets Management
# Auto-generated

playbooks:
- control_id: "SECRETSF-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary secrets in configuration files control patterns for secrets-file-exposure"
  blast_radius_notes: "Automated fix for SECRETSF-01 - Primary Secrets in Configuration Files Control"
  verification_checks:
    - "Verify SECRETSF-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSF-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for secrets in configuration files patterns for secrets-file-exposure"
  blast_radius_notes: "Automated fix for SECRETSF-02 - Input Validation for Secrets in Configuration Files"
  verification_checks:
    - "Verify SECRETSF-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSF-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for secrets in configuration files patterns for secrets-file-exposure"
  blast_radius_notes: "Automated fix for SECRETSF-03 - Configuration Hardening for Secrets in Configuration Files"
  verification_checks:
    - "Verify SECRETSF-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSF-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for secrets in configuration files patterns for secrets-file-exposure"
  blast_radius_notes: "Automated fix for SECRETSF-04 - Monitoring and Logging for Secrets in Configuration Files"
  verification_checks:
    - "Verify SECRETSF-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSF-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for secrets in configuration files patterns for secrets-file-exposure"
  blast_radius_notes: "Automated fix for SECRETSF-05 - Error Handling for Secrets in Configuration Files"
  verification_checks:
    - "Verify SECRETSF-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
