# Playbook for: config-yaml-secrets
# Source: Configuration Security
# Auto-generated

playbooks:
- control_id: "CONFIGYA-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary yaml configuration secrets control patterns for config-yaml-secrets"
  blast_radius_notes: "Automated fix for CONFIGYA-01 - Primary YAML Configuration Secrets Control"
  verification_checks:
    - "Verify CONFIGYA-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGYA-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for yaml configuration secrets patterns for config-yaml-secrets"
  blast_radius_notes: "Automated fix for CONFIGYA-02 - Input Validation for YAML Configuration Secrets"
  verification_checks:
    - "Verify CONFIGYA-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGYA-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for yaml configuration secrets patterns for config-yaml-secrets"
  blast_radius_notes: "Automated fix for CONFIGYA-03 - Configuration Hardening for YAML Configuration Secrets"
  verification_checks:
    - "Verify CONFIGYA-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGYA-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for yaml configuration secrets patterns for config-yaml-secrets"
  blast_radius_notes: "Automated fix for CONFIGYA-04 - Monitoring and Logging for YAML Configuration Secrets"
  verification_checks:
    - "Verify CONFIGYA-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGYA-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for yaml configuration secrets patterns for config-yaml-secrets"
  blast_radius_notes: "Automated fix for CONFIGYA-05 - Error Handling for YAML Configuration Secrets"
  verification_checks:
    - "Verify CONFIGYA-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
