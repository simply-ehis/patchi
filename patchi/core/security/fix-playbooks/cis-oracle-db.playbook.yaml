# Playbook for: cis-oracle-db
# Source: CIS Oracle Benchmark
# Auto-generated

playbooks:
- control_id: "CISORACL-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary oracle database hardening control patterns for cis-oracle-db"
  blast_radius_notes: "Automated fix for CISORACL-01 - Primary Oracle Database Hardening Control"
  verification_checks:
    - "Verify CISORACL-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISORACL-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for oracle database hardening patterns for cis-oracle-db"
  blast_radius_notes: "Automated fix for CISORACL-02 - Input Validation for Oracle Database Hardening"
  verification_checks:
    - "Verify CISORACL-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISORACL-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for oracle database hardening patterns for cis-oracle-db"
  blast_radius_notes: "Automated fix for CISORACL-03 - Configuration Hardening for Oracle Database Hardening"
  verification_checks:
    - "Verify CISORACL-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISORACL-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for oracle database hardening patterns for cis-oracle-db"
  blast_radius_notes: "Automated fix for CISORACL-04 - Monitoring and Logging for Oracle Database Hardening"
  verification_checks:
    - "Verify CISORACL-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISORACL-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for oracle database hardening patterns for cis-oracle-db"
  blast_radius_notes: "Automated fix for CISORACL-05 - Error Handling for Oracle Database Hardening"
  verification_checks:
    - "Verify CISORACL-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
