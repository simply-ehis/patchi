# Playbook for: config-json-secrets
# Source: Configuration Security
# Auto-generated

playbooks:
- control_id: "CONFIGJS-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary json configuration secrets control patterns for config-json-secrets"
  blast_radius_notes: "Automated fix for CONFIGJS-01 - Primary JSON Configuration Secrets Control"
  verification_checks:
    - "Verify CONFIGJS-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGJS-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for json configuration secrets patterns for config-json-secrets"
  blast_radius_notes: "Automated fix for CONFIGJS-02 - Input Validation for JSON Configuration Secrets"
  verification_checks:
    - "Verify CONFIGJS-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGJS-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for json configuration secrets patterns for config-json-secrets"
  blast_radius_notes: "Automated fix for CONFIGJS-03 - Configuration Hardening for JSON Configuration Secrets"
  verification_checks:
    - "Verify CONFIGJS-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGJS-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for json configuration secrets patterns for config-json-secrets"
  blast_radius_notes: "Automated fix for CONFIGJS-04 - Monitoring and Logging for JSON Configuration Secrets"
  verification_checks:
    - "Verify CONFIGJS-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CONFIGJS-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for json configuration secrets patterns for config-json-secrets"
  blast_radius_notes: "Automated fix for CONFIGJS-05 - Error Handling for JSON Configuration Secrets"
  verification_checks:
    - "Verify CONFIGJS-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
