# Playbook for: mitre-persistence-create-modify-process
# Source: MITRE ATT&CK
# Auto-generated

playbooks:
- control_id: "MITREPER-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary create or modify system process (t1543) control patterns for mitre-persistence-create-modify-process"
  blast_radius_notes: "Automated fix for MITREPER-01 - Primary Create or Modify System Process (T1543) Control"
  verification_checks:
    - "Verify MITREPER-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for create or modify system process (t1543) patterns for mitre-persistence-create-modify-process"
  blast_radius_notes: "Automated fix for MITREPER-02 - Input Validation for Create or Modify System Process (T1543)"
  verification_checks:
    - "Verify MITREPER-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for create or modify system process (t1543) patterns for mitre-persistence-create-modify-process"
  blast_radius_notes: "Automated fix for MITREPER-03 - Configuration Hardening for Create or Modify System Process (T1543)"
  verification_checks:
    - "Verify MITREPER-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for create or modify system process (t1543) patterns for mitre-persistence-create-modify-process"
  blast_radius_notes: "Automated fix for MITREPER-04 - Monitoring and Logging for Create or Modify System Process (T1543)"
  verification_checks:
    - "Verify MITREPER-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for create or modify system process (t1543) patterns for mitre-persistence-create-modify-process"
  blast_radius_notes: "Automated fix for MITREPER-05 - Error Handling for Create or Modify System Process (T1543)"
  verification_checks:
    - "Verify MITREPER-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
