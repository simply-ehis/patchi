# Playbook for: mitre-persistence-scheduled-task
# Source: MITRE ATT&CK
# Auto-generated

playbooks:
- control_id: "MITREPER-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary scheduled task/job (t1053) control patterns for mitre-persistence-scheduled-task"
  blast_radius_notes: "Automated fix for MITREPER-01 - Primary Scheduled Task/Job (T1053) Control"
  verification_checks:
    - "Verify MITREPER-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for scheduled task/job (t1053) patterns for mitre-persistence-scheduled-task"
  blast_radius_notes: "Automated fix for MITREPER-02 - Input Validation for Scheduled Task/Job (T1053)"
  verification_checks:
    - "Verify MITREPER-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for scheduled task/job (t1053) patterns for mitre-persistence-scheduled-task"
  blast_radius_notes: "Automated fix for MITREPER-03 - Configuration Hardening for Scheduled Task/Job (T1053)"
  verification_checks:
    - "Verify MITREPER-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for scheduled task/job (t1053) patterns for mitre-persistence-scheduled-task"
  blast_radius_notes: "Automated fix for MITREPER-04 - Monitoring and Logging for Scheduled Task/Job (T1053)"
  verification_checks:
    - "Verify MITREPER-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "MITREPER-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for scheduled task/job (t1053) patterns for mitre-persistence-scheduled-task"
  blast_radius_notes: "Automated fix for MITREPER-05 - Error Handling for Scheduled Task/Job (T1053)"
  verification_checks:
    - "Verify MITREPER-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
