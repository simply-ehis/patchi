# Playbook for: django-debug-mode
# Source: Django Security
# Auto-generated

playbooks:
- control_id: "DJANGODE-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary django debug=true in production control patterns for django-debug-mode"
  blast_radius_notes: "Automated fix for DJANGODE-01 - Primary Django DEBUG=True in Production Control"
  verification_checks:
    - "Verify DJANGODE-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "DJANGODE-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for django debug=true in production patterns for django-debug-mode"
  blast_radius_notes: "Automated fix for DJANGODE-02 - Input Validation for Django DEBUG=True in Production"
  verification_checks:
    - "Verify DJANGODE-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "DJANGODE-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for django debug=true in production patterns for django-debug-mode"
  blast_radius_notes: "Automated fix for DJANGODE-03 - Configuration Hardening for Django DEBUG=True in Production"
  verification_checks:
    - "Verify DJANGODE-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "DJANGODE-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for django debug=true in production patterns for django-debug-mode"
  blast_radius_notes: "Automated fix for DJANGODE-04 - Monitoring and Logging for Django DEBUG=True in Production"
  verification_checks:
    - "Verify DJANGODE-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "DJANGODE-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for django debug=true in production patterns for django-debug-mode"
  blast_radius_notes: "Automated fix for DJANGODE-05 - Error Handling for Django DEBUG=True in Production"
  verification_checks:
    - "Verify DJANGODE-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
