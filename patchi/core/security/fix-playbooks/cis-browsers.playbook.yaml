# Playbook for: cis-browsers
# Source: CIS Browser Benchmark
# Auto-generated

playbooks:
- control_id: "CISBROWS-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary web browser hardening control patterns for cis-browsers"
  blast_radius_notes: "Automated fix for CISBROWS-01 - Primary Web Browser Hardening Control"
  verification_checks:
    - "Verify CISBROWS-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISBROWS-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for web browser hardening patterns for cis-browsers"
  blast_radius_notes: "Automated fix for CISBROWS-02 - Input Validation for Web Browser Hardening"
  verification_checks:
    - "Verify CISBROWS-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISBROWS-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for web browser hardening patterns for cis-browsers"
  blast_radius_notes: "Automated fix for CISBROWS-03 - Configuration Hardening for Web Browser Hardening"
  verification_checks:
    - "Verify CISBROWS-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISBROWS-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for web browser hardening patterns for cis-browsers"
  blast_radius_notes: "Automated fix for CISBROWS-04 - Monitoring and Logging for Web Browser Hardening"
  verification_checks:
    - "Verify CISBROWS-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISBROWS-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for web browser hardening patterns for cis-browsers"
  blast_radius_notes: "Automated fix for CISBROWS-05 - Error Handling for Web Browser Hardening"
  verification_checks:
    - "Verify CISBROWS-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
