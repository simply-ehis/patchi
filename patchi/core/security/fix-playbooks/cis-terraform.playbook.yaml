# Playbook for: cis-terraform
# Source: CIS Terraform Benchmark
# Auto-generated

playbooks:
- control_id: "CISTERRA-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary terraform configuration hardening control patterns for cis-terraform"
  blast_radius_notes: "Automated fix for CISTERRA-01 - Primary Terraform Configuration Hardening Control"
  verification_checks:
    - "Verify CISTERRA-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISTERRA-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for terraform configuration hardening patterns for cis-terraform"
  blast_radius_notes: "Automated fix for CISTERRA-02 - Input Validation for Terraform Configuration Hardening"
  verification_checks:
    - "Verify CISTERRA-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISTERRA-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for terraform configuration hardening patterns for cis-terraform"
  blast_radius_notes: "Automated fix for CISTERRA-03 - Configuration Hardening for Terraform Configuration Hardening"
  verification_checks:
    - "Verify CISTERRA-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISTERRA-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for terraform configuration hardening patterns for cis-terraform"
  blast_radius_notes: "Automated fix for CISTERRA-04 - Monitoring and Logging for Terraform Configuration Hardening"
  verification_checks:
    - "Verify CISTERRA-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "CISTERRA-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for terraform configuration hardening patterns for cis-terraform"
  blast_radius_notes: "Automated fix for CISTERRA-05 - Error Handling for Terraform Configuration Hardening"
  verification_checks:
    - "Verify CISTERRA-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
