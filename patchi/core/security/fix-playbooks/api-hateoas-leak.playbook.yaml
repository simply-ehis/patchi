# Playbook for: api-hateoas-leak
# Source: API Design Security
# Auto-generated

playbooks:
- control_id: "APIHATEO-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary api hateoas information leak control patterns for api-hateoas-leak"
  blast_radius_notes: "Automated fix for APIHATEO-01 - Primary API HATEOAS Information Leak Control"
  verification_checks:
    - "Verify APIHATEO-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "APIHATEO-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for api hateoas information leak patterns for api-hateoas-leak"
  blast_radius_notes: "Automated fix for APIHATEO-02 - Input Validation for API HATEOAS Information Leak"
  verification_checks:
    - "Verify APIHATEO-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "APIHATEO-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for api hateoas information leak patterns for api-hateoas-leak"
  blast_radius_notes: "Automated fix for APIHATEO-03 - Configuration Hardening for API HATEOAS Information Leak"
  verification_checks:
    - "Verify APIHATEO-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "APIHATEO-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for api hateoas information leak patterns for api-hateoas-leak"
  blast_radius_notes: "Automated fix for APIHATEO-04 - Monitoring and Logging for API HATEOAS Information Leak"
  verification_checks:
    - "Verify APIHATEO-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "APIHATEO-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for api hateoas information leak patterns for api-hateoas-leak"
  blast_radius_notes: "Automated fix for APIHATEO-05 - Error Handling for API HATEOAS Information Leak"
  verification_checks:
    - "Verify APIHATEO-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
