# Playbook for: secrets-in-transit
# Source: Secrets Management
# Auto-generated

playbooks:
- control_id: "SECRETSI-01"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure primary secrets in transit control patterns for secrets-in-transit"
  blast_radius_notes: "Automated fix for SECRETSI-01 - Primary Secrets in Transit Control"
  verification_checks:
    - "Verify SECRETSI-01 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSI-02"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "bandit"
  human_review_required: false
  llm_fix_template: "Apply secure input validation for secrets in transit patterns for secrets-in-transit"
  blast_radius_notes: "Automated fix for SECRETSI-02 - Input Validation for Secrets in Transit"
  verification_checks:
    - "Verify SECRETSI-02 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSI-03"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "ruff"
  human_review_required: false
  llm_fix_template: "Apply secure configuration hardening for secrets in transit patterns for secrets-in-transit"
  blast_radius_notes: "Automated fix for SECRETSI-03 - Configuration Hardening for Secrets in Transit"
  verification_checks:
    - "Verify SECRETSI-03 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSI-04"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure monitoring and logging for secrets in transit patterns for secrets-in-transit"
  blast_radius_notes: "Automated fix for SECRETSI-04 - Monitoring and Logging for Secrets in Transit"
  verification_checks:
    - "Verify SECRETSI-04 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
- control_id: "SECRETSI-05"
  playbook_version: "1.0.0"
  fix_strategy: "deterministic"
  deterministic_tool: "semgrep"
  human_review_required: false
  llm_fix_template: "Apply secure error handling for secrets in transit patterns for secrets-in-transit"
  blast_radius_notes: "Automated fix for SECRETSI-05 - Error Handling for Secrets in Transit"
  verification_checks:
    - "Verify SECRETSI-05 is addressed"
    - "Run tests to confirm no regression"
    - "Scan with security linter to confirm fix"
