# Pending case: 32-vs-74 (AGENT_FEEDBACK.md:21, spec §3)

## What was reported
- `DuplicateScanner` produced 5704 raw findings on an unnamed project.
- `NoiseFilter` (tests=discard) + `ConfidenceGate` low-tier discard reduced that to **32 kept** (29 medium + 3 high).
- The user reported **74 real** findings. The note ends in "may need tuning" — nobody confirmed which number is right.

## Status
- **DuplicateScanner is deleted** (spec: "delete the duplicate scanners"). The exact 5704-noise source cannot recur, and this exact case cannot be re-run.
- **Discard audit trail added**: every `DetectionPipeline.process()` run now persists `.patchi/gate_audit.json` with raw_in, per-layer noise/gate counts, every discarded finding's key + reason (first 500), and the exact gate config. The next "user says N" dispute is diffable in one command.
- **Ground truth still missing**: the 74 findings (or the project + revision they came from) were never recorded. Without them, precision/recall against this case cannot be computed.

## To close this case
1. Provide the 74 findings (file + type + line) or the project revision.
2. Add them as `evals/cases/gate/g74_*.json` entries with `kind: vuln` (or `clean` for any the gate was right to drop).
3. Run `p eval gate` — routing accuracy + vuln recall must cover them, or thresholds get tuned with the numbers on the table.
